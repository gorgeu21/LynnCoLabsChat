version: 1.2.1  # ← ТОЛЬКО ОДИН version!
cache: false

endpoints:
  # Отключаем ВСЕ встроенные
  openai:
    disabled: true
  anthropic:
    disabled: true
  google:
    disabled: true
  azure:
    disabled: true
  ollama:
    disabled: true

  custom:
    - name: "🟢 LynnCoLabs AI"
      apiKey: "${OPENROUTER_KEY}"  # ← ${OPENROUTER_KEY} НЕ API_KEY!
      baseURL: "https://openrouter.ai/api/v1"
      headers:
        x-librechat-body-parentmessageid: '{{LIBRECHAT_BODY_PARENTMESSAGEID}}'
      models:
        default: 
          - "anthropic/claude-3.5-sonnet"
          - "openai/gpt-4o-mini"
        fetch: true
      titleConvo: true
      titleModel: "anthropic/claude-3.5-sonnet"
      dropParams: ["stop"]
      modelDisplayLabel: "🚀 OpenRouter"
